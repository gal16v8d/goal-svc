logging:
  level:
    org:
      hibernate:
        SQL: error
      springframework: off
    root: error

service:
  welcome:
    message: Hello from test goal-svc
    
spring:
  cloud:
    compatibility-verifier:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false
