logging:
  level:
    org:
      hibernate:
        SQL: error
      springframework: off
    root: error

service:
  welcome:
    message: Hello from test goal-svc
    
spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false
